<UserControl
    x:Class="InternetTest.Views.Oobe.FeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:InternetTest.Properties"
    xmlns:local="clr-namespace:InternetTest.Views.Oobe"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="500"
    d:DesignWidth="800"
    FontFamily="../../Fonts/#Hauora"
    Foreground="{DynamicResource Foreground1}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="24"
                FontWeight="ExtraBold"
                Text="{x:Static lang:Resources.Features}" />
            <TextBlock
                Margin="0 0 0 15"
                HorizontalAlignment="Center"
                Text="{x:Static lang:Resources.HomeWelcomeDesc}" />
            <Grid Grid.Row="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Border
                    x:Name="DownDetectorBorder"
                    Margin="5"
                    Padding="10 5"
                    HorizontalAlignment="Center"
                    Background="{DynamicResource CardBackground}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="1"
                    CornerRadius="10">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="../../Fonts/#FluentSystemIcons-Filled"
                            FontSize="24"
                            Foreground="{DynamicResource Accent}"
                            Text="&#xFB71;" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="10 0 0 0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontWeight="ExtraBold"
                            Text="{x:Static lang:Resources.DownDetector}" />

                        <TextBlock
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Text="{x:Static lang:Resources.DownDetectorInfo}"
                            TextWrapping="Wrap" />
                    </Grid>
                </Border>

                <Border
                    x:Name="LocateIpBorder"
                    Grid.Column="1"
                    Margin="5"
                    Padding="10 5"
                    HorizontalAlignment="Center"
                    Background="{DynamicResource CardBackground}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="1"
                    CornerRadius="10">

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="../../Fonts/#FluentSystemIcons-Filled"
                            FontSize="24"
                            Foreground="{DynamicResource Accent}"
                            Text="&#xF503;" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="10 0 0 0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontWeight="ExtraBold"
                            Text="{x:Static lang:Resources.LocateAnIP}" />

                        <TextBlock
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Text="{x:Static lang:Resources.LocateIPInfo}"
                            TextWrapping="Wrap" />
                    </Grid>
                </Border>

                <Border
                    x:Name="WiFiBorder"
                    Grid.Column="2"
                    Margin="5"
                    Padding="10 5"
                    HorizontalAlignment="Center"
                    Background="{DynamicResource CardBackground}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="1"
                    CornerRadius="10">
                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="../../Fonts/#FluentSystemIcons-Filled"
                            FontSize="24"
                            Foreground="{DynamicResource Accent}"
                            Text="&#xF8CC;" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="10 0 0 0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontWeight="ExtraBold"
                            Text="{x:Static lang:Resources.WifiPasswords}" />

                        <TextBlock
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Text="{x:Static lang:Resources.WiFiPasswordsInfo}"
                            TextWrapping="Wrap" />
                    </Grid>
                </Border>
            </Grid>
        </StackPanel>
        <Button
            x:Name="NextBtn"
            Grid.Row="2"
            Margin="5 10"
            Padding="10 5"
            HorizontalAlignment="Center"
            Background="{DynamicResource Accent}"
            BorderThickness="0"
            Command="{Binding NextCommand}"
            Content="{x:Static lang:Resources.Next}"
            Cursor="Hand"
            FontWeight="ExtraBold"
            Foreground="{DynamicResource WindowButtonsHoverForeground1}" />

    </Grid>
</UserControl>
