<controls:MicaWindow
    x:Class="InternetTest.Windows.OobeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MicaWPF.Lite.Controls;assembly=MicaWPF.Lite"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:InternetTest.Properties"
    xmlns:local="clr-namespace:InternetTest.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:InternetTest.Views.Oobe"
    xmlns:vm="clr-namespace:InternetTest.ViewModels.Oobe"
    Title="{x:Static lang:Resources.InternetTestPro}"
    Width="950"
    Height="600"
    d:Background="Transparent"
    FontFamily="../Fonts/#Hauora"
    Foreground="{DynamicResource Foreground1}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vm:WelcomePageViewModel}">
                <views:WelcomePage />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:FeaturesPageViewModel}">
                <views:FeaturesPage />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ThemePageViewModel}">
                <views:ThemePage />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:JumpInPageViewModel}">
                <views:JumpInPage />
            </DataTemplate>
        </Grid.Resources>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </ScrollViewer>
    </Grid>
</controls:MicaWindow>
